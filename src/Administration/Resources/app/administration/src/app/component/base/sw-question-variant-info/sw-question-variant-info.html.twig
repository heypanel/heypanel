{% block sw_question_variant_info %}
<span
    v-tooltip="{
        message: helpText,
        width: tooltipWidth,
        disabled: !showTooltip || !(helpText && helpText.length > 0)
    }"
    class="sw-question-variant-info"
>
    <span class="sw-question-variant-info__text">

        {% block sw_question_variant_info_question_name %}
        <span class="sw-question-variant-info__question-name">
            <sw-highlight-text
                v-if="highlighted"
                :text="getFirstSlot()"
                :search-term="searchTerm"
            />
            <slot v-else></slot>
        </span>
        {% endblock %}

        {% block sw_question_variant_info_options %}
        <template v-if="variations && variations.length > 0">
            <span
                v-for="(variation, index) in variations"
                :key="variation.group"
                class="sw-question-variant-info__specification"
            >
                <template v-if="index === 0 && !ommitParenthesis">(</template>

                {% block sw_question_variant_info_options_text %}
                <span>{{ variation.group }}</span>: {{ variation.option }}
                {% endblock %}

                <template v-if="index !== Object.keys(variations).length - 1">{{ seperator }}</template>

                <template v-if="index === Object.keys(variations).length - 1 && !ommitParenthesis">)</template>
            </span>
        </template>
        {% endblock %}

    </span>
</span>
{% endblock %}
