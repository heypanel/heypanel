{% block sw_channel_detail_analytics %}
<mt-card
    position-identifier="sw-channel-detail-analytics"
    :title="$tc('sw-channel.detail.analytics.cardTitle')"
    :is-loading="isLoading"
>
    <template v-if="channel && channel.analytics">
        {% block sw_channel_detail_analytics_headline %}
        <div class="sw-channel-detail-analytics__headline-text">
            {{ $tc('sw-channel.detail.analytics.headline') }}
        </div>
        {% endblock %}

        {% block sw_channel_detail_analytics_description %}
        <mt-text
            size="xs"
            class="sw-channel-detail-analytics__description"
            v-html="$tc('sw-channel.detail.analytics.description')"
        ></mt-text>
        {% endblock %}

        {% block sw_channel_detail_analytics_fields_tracking_id %}

        <mt-text-field
            v-model="channel.analytics.trackingId"
            :label="$tc('sw-channel.detail.analytics.fields.trackingId.label')"
            :placeholder="$tc('sw-channel.detail.analytics.fields.trackingId.placeHolder')"
            name="trackingId"
            :disabled="!acl.can('channel.editor')"
            :help-text="$tc('sw-channel.detail.analytics.fields.trackingId.helpText')"
        />
        {% endblock %}

        {% block sw_channel_detail_analytics_fields_container %}
        <sw-container
            columns="repeat(auto-fit, minmax(250px, 1fr))"
            gap="0 30px"
        >
            {% block sw_channel_detail_analytics_fields_active %}

            <mt-switch
                v-model="channel.analytics.active"
                :label="$tc('sw-channel.detail.analytics.fields.active.label')"
                name="analyticsActive"
                :disabled="!acl.can('channel.editor')"
            />
            {% endblock %}

            {% block sw_channel_detail_analytics_fields_track_orders %}

            <mt-switch
                v-model="channel.analytics.trackOrders"
                :label="$tc('sw-channel.detail.analytics.fields.trackOrders.label')"
                name="trackOrders"
                :disabled="!acl.can('channel.editor')"
            />
            {% endblock %}

            {% block sw_channel_detail_analytics_fields_anonymize_ip %}

            <mt-switch
                v-model="channel.analytics.anonymizeIp"
                :label="$tc('sw-channel.detail.analytics.fields.anonymizeIp.label')"
                name="anonymizeIp"
                :disabled="!acl.can('channel.editor')"
            />
            {% endblock %}
        </sw-container>
        {% endblock %}
    </template>
</mt-card>
{% endblock %}
